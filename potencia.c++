#include <bits/stdc++.h>
#include <cmath>

typedef long long ll;
using namespace std;

int main() {
  long double x, y;
  cin >> x >> y;
  cout << fixed << setprecision(4) << pow(x, y);
  return 0;
}
